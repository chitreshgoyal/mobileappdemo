<% if target.errors.any? %>
<div id="alert alert-error">
	<a class="close" data-dismiss="alert" href="#">x</a>
	<strong><%= pluralize(target.errors.count, "Error") %></strong>
	<ul>
		<% target.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>
