<h1>Listing products</h1>

<%= table_for(@products) do |product|
  product.data :actions=>:all
 end %>


<table width="505">
	<% @products.each do |product| %>
	<tr>
	<td><%= link_to "#{product.name} #{product.sku} product Route", chitresh_url(:name=>"#{product.name}", :id=>"#{product.id}") %></td>
	</tr>
	<% end %>
</table>
<%# @a =Time.now%>
<%#= debug sort_data(@products) %>
<%#= Time.now-@a %>
<%#= debug sort_data(@products) %>


